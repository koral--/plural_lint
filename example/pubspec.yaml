name: app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  lint: ^2.1.2
  custom_lint: ^0.5.4
  plural_lint:
    path: ../

flutter:
  uses-material-design: true
    
flutter_intl:
  enabled: true
  class_name: Strings
  main_locale: fr
